<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <!--
        status="WARN": Log4j2 自身的日志输出级别（仅用来调试日志系统本身的配置）
        monitorInterval="30": 每隔 30 秒自动检测配置文件是否有修改，若有修改会重新加载
    -->
    <Appenders>
        <!-- 控制台输出 -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%highlight{%-5level}] [%thread] [traceId=%X{traceId}] %logger{36} - %msg%n" />
        </Console>

        <!-- 轮盘日志 -->
        <RollingFile name="File"
                     fileName="logs/app.log"
                     filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%highlight{%-5level}] [%thread] [traceId=%X{traceId}] %logger{36} - %msg%n" />
            <Policies>
                <!-- 时间滚动策略，每日创建一个日志文件 -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <!-- 大小滚动策略，单个文件达到 10MB 切分 -->
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <!-- 控制保留多少历史文件，超过数量会自动删除 -->
            <DefaultRolloverStrategy max="30" />
        </RollingFile>
    </Appenders>

    <!-- 2. 配置日志记录器（Logger） -->
    <Loggers>
        <!--
            项目自己的日志级别。
            原来是 debug —— 开发时方便，但压测时每个请求都要写好几行日志，
            同步 IO 会直接吞掉吞吐量。默认改成 info。
            要看 MyBatis 的 SQL 时，把这里改回 debug 即可
            （配合 application.yaml 里 log-impl = Slf4jImpl，SQL 才会走这里）。
        -->
        <Logger name="com.studynote" level="info" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
        </Logger>

        <!--
            临时诊断：CORS 拒绝时 DefaultCorsProcessor 会在这里打一行
            "Reject: 'xxx' origin is not allowed"。
            这是排查 403 的【唯一】线索 —— 因为 CorsFilter 在 DispatcherServlet
            之前就返回了，com.studynote 的日志里根本不会留下任何记录。
            查完把 level 改回 info（或删掉这个 Logger）。
        -->
        <Logger name="org.springframework.web.cors" level="debug" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
        </Logger>

        <!-- 3. Root Logger，默认日志级别为 info -->
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
        </Root>
    </Loggers>
</Configuration>
