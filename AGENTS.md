# 接手入口

先阅读根目录 `MIGRATION_HANDOFF.md` 顶部最新交接，再继续前端 React → Vue 迁移。所有页面已实现，此前构建通过；最后一批修改尚未完整复验，真实后端联调未完成。不要根据旧批次记录误判页面仍缺失，也不要宣称已全部验收。

每完成一批实质修改、运行验证或停止工作前，都更新 `MIGRATION_HANDOFF.md` 中的进度、检查结果和下一步。明确区分“已写代码”“已通过验证”“尚未完成”，将新的阻塞和决定写入文件，不要仅留在聊天记录中。

用户要求删除项目作者信息及代码注释。交接 Markdown 是用户明确要求的工作记录，须保留。默认保留社区用户笔记/评论的发布者业务字段；作者清理范围及是否清理后端注释尚未得到用户明确答复。

尽量保持原功能与接口，先完成可运行的 Vue 迁移；没有额外授权不要自行重做产品设计、修改后端业务或部署到远程服务器。禁止重新执行旧源码清理脚本，该脚本已经删除，重复执行会删除新 Vue 文件。

本项目当前没有 Git 仓库。不要假设被移除的 React 源码可以从 Git 恢复。后续变更优先使用小步修改与验证。
